# 税理士ワークスペース Phase 7 設計書 ―「MFの補助」への再定義

作成: 2026-08-04 / 状態: **設計とモックのみ。実装は未着手**

前提となる文書:
- `docs/TAX_WORKSPACE_PLAN.md`（Phase 1〜4。実装済み）
- `docs/TAX_WORKSPACE_PHASE5_PLAN.md`（Phase 5。設計のみ）
- `docs/TAX_WORKSPACE_PHASE6_PLAN.md`（Phase 6。設計のみ。3視点レビュー2巡を通過）

この改訂の出発点は利用者の次の指摘（2026-08-04）:

> あくまでMFを作成したいんじゃなくてMFの補助みたいな立ち位置なので、
> 特に税理士は顧客から資料をもらって登録するとおもうんだけど、それの最大限補助というか楽になるように

Phase 1〜6 は「**MFの未仕訳明細を、この画面から登録する**」という設計だった。
これは MF の中核機能をなぞる方向で、**MF より速くなることが構造的に難しい**。
実際、中堅レビューは一貫して「MFで直接やったほうが速い場面がある」と指摘し続けていた。

Phase 7 は**方向を変える**。この文書も**根拠のある事実**と**未確認の事項**を分けて書く。

---

## 0. 立ち位置の再定義

### 0.1 税理士の1ヶ月を工程に分解する

| # | 工程 | 現状の負担 | MFの支援 | この画面がやること |
|---|---|---|---|---|
| ① | **資料を集める・催促する** | メール・電話・待ち時間 | **なし** | ★A 資料の受け取りボード |
| ② | 資料と明細を突き合わせる | 目視で照合 | Boxに置けるだけ | 既存のマッチング（Phase 1〜） |
| ③ | 仕訳を登録する | 入力 | **◎ 最も得意** | 提案・一括確認で補助（Phase 6） |
| ④ | **分からないものを顧客に聞く** | メール往復・紐付けが消える | **なし** | ★B 質問と回答 |
| ⑤ | **月次でチェックする** | 経験と目視 | 試算表を出すだけ | ★C 月次チェック |
| ⑥ | 間違いを直す | MFで削除・再入力 | ◎ | ★E 取り消し |

**MFが担っているのは③だけ。①④⑤が丸ごと空いている。**

### 0.2 この構成でしか作れないもの

税理士向けのSaaSは、**顧客側の画面を持てない**。顧客にログインしてもらうこと自体が
導入の壁になるからである。

一方このシステムは、**すでに顧客（RIBRE）側の画面である**。
証憑インボックス（`mf-evidence.html`）・売上管理（`index.html`）・電子古物台帳が
同じログインの中にある。

> つまり **「税理士が質問を投げると顧客に届き、顧客の答えがその取引の行に残る」**
> という往復は、この構成でしか成立しない。ここを中心に据える。

### 0.3 やらないことの宣言（設計の背骨）

**MFでやったほうが速いことは、MFへのリンクを出して終わりにする。**

この方針を画面にも明記する。具体的には:

- 帳簿の閲覧・検索・決算・申告 → MFへ
- 仕訳の詳細な修正（借方貸方の付け替え等） → MFへ
- 大量の定型入力（毎月同じ仕訳） → MFの仕訳辞書・自動仕訳ルールへ
- **この画面は「登録する前」と「登録した後」を引き受ける**

⚠ Phase 6 までの一括確認リストも、この方針の下では
**「MFに勝つための機能」ではなく「資料が揃った分をまとめて片付ける機能」**と位置づけ直す。

---

## 1. MF API の棚卸し（実測）

出典: `https://developers.api-accounting.moneyforward.com/v3/openapi.yaml`（2026-08-04取得・213,481文字）
および MF公式MCPサーバー経由の**実データ照会**（読み取りのみ。データは一切変更していない）。

**全23エンドポイント中、現在使っているのは10。残り13のうち6つは価値が高い。**

| エンドポイント | scope | 現状 | Phase 7 での用途 |
|---|---|---|---|
| GET /accounts | accounts.read | 使用中 | — |
| GET /taxes | taxes.read | 使用中 | — |
| GET /journals | journal.read | 使用中 | — |
| GET /journals/{id} | journal.read | 未使用 | 取り消し前の内容確認 |
| POST /journals | journal.write | 使用中 | — |
| **DELETE /journals/{id}** | journal.write | **未使用** | ★E **登録の取り消し** |
| PUT /journals/{id} | journal.write | 未使用（見送り継続） | 全置換APIのため使わない |
| GET /transactions | transaction.read | 使用中 | ★A `voucher_file_ids` を使う |
| **POST /transactions** | **transaction.write** | **未使用** | ★D **現金払いの明細を作る** |
| POST /transactions/journalize | journal.write | 使用中 | — |
| POST /vouchers | voucher.write | 使用中 | — |
| DELETE /vouchers | voucher.write | 使用中 | — |
| GET /trade_partners | trade_partners.read | 使用中（0件） | — |
| POST /trade_partners | **trade_partners.write** | 未使用 | 取引先マスタを育てる（優先度低） |
| **GET /connected_accounts** | **connected_account.read** | **未使用** | ★A 口座ごとのデータ到達状況 |
| **GET /term_settings** | **offices.read** | **未使用** | 会計年度・税方式を**推測せず**取得 |
| GET /offices | offices.read | 未使用 | 事業者名の表示 |
| GET /sub_accounts | accounts.read | 未使用 | 補助科目の選択肢 |
| GET /departments | departments.read | 未使用（0件） | — |
| **GET /reports/transition_pl** | **report.read** | **未使用** | ★C **計上漏れ・異常の検知** |
| GET /reports/transition_bs | report.read | 未使用 | ★C 残高の推移 |
| GET /reports/trial_balance_pl | report.read | 未使用 | ★C 月次の試算表 |
| GET /reports/trial_balance_bs | report.read | 未使用 | ★C 月次の試算表 |

### 1.1 追加が必要なスコープ

現在の `MF_SCOPE`（`api/mf/_lib/mf-client.js`）に加えて:

```
mfc/accounting/report.read              ← ★C 月次チェック
mfc/accounting/offices.read             ← 会計年度設定
mfc/accounting/connected_account.read   ← ★A 口座一覧
mfc/accounting/transaction.write        ← ★D 現金払いの明細作成
mfc/accounting/trade_partners.write     ← 取引先マスタ（優先度低）
```

⚠ **再連携（MF連携のやり直し）が1回必要**。Phase 6 までの機能は影響を受けない
（既存トークンのスコープはそのまま有効なため）。**再連携するなら1回で済むよう、
着手時にまとめて追加する**。

### 1.2 実測して分かったこと（すべて実データ）

**`GET /term_settings`** — 3期分が返り、**推測していた前提がすべて裏取りできた**:

```
fiscal_year 2026: 2026-03-01 〜 2027-02-28
accounting_method: TAX_INCLUDED（税込経理）
tax_method: SIMPLE（簡易課税）
sales_rounding_method / purchases_rounding_method: ROUND_UP（切り上げ）
business_types: WHOLESALE, RETAIL（卸売業＋小売業）
prefecture: 埼玉県
```

→ **`closed_term_policy` の会計年度の境目をハードコードする必要がなくなる。**
3/1〜2/28 という前提を、コードに書かずAPIから取れる。

⚠ **ただし「その期が締まっているか（決算確定済みか）」のフラグは返らない。**
確認した限り term_settings にも offices にも存在しない。
**したがって Phase 6 の「決算済み期の警告」は、引き続き手動設定が必要**（推測しない）。

**`GET /offices`** — `株式会社RIBRE` / `CORPORATE` / 従業員数レンジ `1〜5`。
Phase 6 で「事業主借は個人事業主の科目なので不適切」と判断した根拠を、
**APIで自動判定できる**（`type: CORPORATE` なら法人の科目だけを出す）。

**`GET /departments`** — 0件。部門管理は使っていない。**部門の選択欄は作らない。**

---

## 2. ★C 月次チェック（推移表の異常検知）— 実データで効果を検証済み

### 2.1 これが最も価値が高いと判断した理由

税理士が月次で必ずやることは「**先月あった費用が今月も入っているか**」の確認である。
これは経験に頼った目視作業であり、**完全に機械化できる**。

`GET /reports/transition_pl` を実際に呼び、**2026年度3月〜7月の実データ**を取得した。
結果（円。今日は8/4なので、7月がまさに税理士の目の前にある月）:

| 勘定科目 | 3月 | 4月 | 5月 | 6月 | **7月** |
|---|---:|---:|---:|---:|---:|
| 売上高 | 3,377,199 | 2,747,443 | 2,802,275 | 3,168,370 | **393,434** |
| 役員報酬 | 594,000 | 613,000 | 613,000 | 613,000 | **0** |
| 地代家賃 | 198,000 | 198,000 | 198,000 | 198,000 | **0** |
| 減価償却費 | 369,759 | 369,759 | 369,759 | 369,759 | **0** |
| 荷造運賃 | 248,667 | 226,653 | 262,293 | 245,993 | **0** |
| リース料 | 37,950 | 37,950 | 37,950 | 33,000 | **0** |
| 保険料 | 12,020 | 21,200 | 21,200 | 21,200 | **0** |
| 支払手数料【原価】 | 187,163 | 210,428 | 187,055 | 193,721 | **0** |

**4ヶ月連続で計上されている科目が、7月だけ0**。これは「7月がまだ登録途中」という
正常な状態を示しているが、**同じロジックが「登録し忘れ」も検知する**。

さらに同じデータから、金額の異常も見つかった:

- **支払手数料 5月 221,007円** — 他の月は 21,685〜34,535円。**約7〜10倍**
- **諸会費 3月 −16,760円** — 費用科目が**マイナス**。取消・振替の可能性

⚠ これらは**このシステムが検知できるという実証**であって、
「間違っている」という判断ではない。画面でも「確認してください」としか言わない。

### 2.2 画面の設計

**⑨ 月次チェック**タブ（新設）。対象月を選ぶと3つのリストを出す。

**(1) いつもあるのに今月まだ無い科目**

> 過去4ヶ月すべてに計上があり、当月が0の勘定科目を並べる。
> 「地代家賃 — 過去4ヶ月は毎月198,000円。7月はまだ0円です。」
> 各行から**その科目の未仕訳明細を検索**するボタンを置く（①タブへ絞り込み遷移）

判定条件（推測を避けるため明示する）:
- 直近4ヶ月（当月を除く）**すべて**に金額があり、当月が0
- 4ヶ月分のデータが揃っていない科目は**判定しない**（期首直後の誤検知を避ける）

**(2) 金額が普段と大きく違う科目**

> 当月の金額が、過去4ヶ月の**中央値の3倍以上または1/3以下**（かつ差額1万円以上）
> 「支払手数料 — 普段は約28,000円ですが、5月は221,007円です。」

⚠ 平均ではなく**中央値**を使う。1回の外れ値に基準を引っ張られないため。
⚠ 「3倍・1/3・1万円」の値は**画面から変更できるようにする**（事務所ごとに感覚が違う）。

**(3) 符号がおかしい科目**

> 費用科目がマイナス、収益科目がマイナスの行を出す。
> 「諸会費 — 3月が −16,760円です。取消や振替が入っていないか確認してください。」

**(4) 参考: 試算表**

> `trial_balance_bs` / `trial_balance_pl` をそのまま表で見せる。
> **加工しない。** MFの試算表画面へのリンクも併記する。

### 2.3 この機能の安全性

**読み取り専用。何も書き込まない。** スコープも `report.read` のみ。
実装の危険度が最も低く、価値が最も高い。**だから最初に作る。**

---

## 3. ★A 資料の受け取りボード（①の工程）

### 3.1 解く問題

税理士が一番待たされるのは「顧客からの資料」である。そして現状、
**何が足りないのかを税理士が手で調べて、手で催促している**。

このシステムは顧客側にあるので、**足りないものを自動で出し、その場で顧客に頼める**。

### 3.2 「足りない」をどう判定するか（3つの根拠）

**(a) 証憑が付いていない明細**

`GET /transactions` の応答に **`voucher_file_ids`（この明細に紐づく証憑のIDリスト）**
が含まれることを OpenAPI 仕様で確認した。
これが空の明細＝**まだ資料が来ていない取引**。

⚠ **未確認（P7-A）**: 実データで `voucher_file_ids` が実際に埋まるかは未検証。
MCP経由の `getTransactions` は現在 `dependency server internal error` を返すため
（`connected_accounts` も同様。**アプリ側のトークンは `transaction.read` を持ち
本番で動作しているので、MCP側のスコープ不足と考えられるが断定はしない**）、
**実装の最初にアプリ側のトークンで実測する。**空振りなら、この判定は
既存の `mf_evidence` 台帳側の突合に切り替える（後述の代替案）。

代替案（`voucher_file_ids` が使えなかった場合）: 台帳 `mf_evidence` の
`status='attached'` レコードが持つ仕訳IDから逆引きし、明細IDと突き合わせる。
Phase 1 のマッチング結果を再利用するだけなので、追加のAPIは要らない。

**(b) 口座ごとのデータが途中で止まっている**

`GET /connected_accounts` で連携サービスの一覧を取り、
`GET /transactions` を口座ごとに引いて**最新の取引日**を出す。

> 「〇〇カード — 7/18以降の明細が来ていません（他の口座は7/31まで）」

⚠ `ConnectedAccount` のスキーマに**最終同期日時のフィールドは無い**（仕様で確認）。
したがって「同期エラー」を直接は取れない。**明細の最新日から推定するだけ**であり、
画面でもそう書く（「同期が止まっています」ではなく「明細が来ていません」）。

**(c) 現金払い**

口座・カードを通らない支払いは、そもそも明細が存在しない。
これは自動検知できない。→ ★D で扱う。

### 3.3 画面の設計

**⑩ 資料の受け取り**タブ（新設）。上から:

```
7月の資料の状況
　証憑が付いている明細   68件
　まだ資料が無い明細     12件  ← ここを減らすのが仕事
　資料はあるが未仕訳      5件

【まだ資料が無い明細】
 7/22  ｱﾏｿﾞﾝ ｼﾞｬﾊﾟﾝ      3,480円   [顧客に頼む] [証憑不要にする]
 7/25  ﾀｸｼｰ                1,200円   [顧客に頼む] [証憑不要にする]
 …
　　　　　　　　　　　　　[まとめて顧客に依頼する（12件）]

【口座ごとの最新の明細】
 楽天銀行      7/31 まで ✓
 三井住友カード 7/31 まで ✓
 ○○カード     7/18 まで  ← 13日分ありません
```

**[顧客に頼む]** を押すと ★B の質問として顧客側に届く（同じ仕組みを使う）。

⚠ **「証憑不要にする」は Phase 5 ⑧の設計をそのまま使う。**
振込手数料・受取利息など、性質上そもそも領収書が存在しない取引を消し込むため。
これが無いとリストが永久に減らず、機能全体が信用されなくなる。

---

## 4. ★B 質問と回答（④の工程）― この設計の中核

### 4.1 解く問題

税理士「7/22の3,480円は何の支払いですか？」
→ メールを書く → 顧客が返信する → **税理士がその返信をMFの摘要に手で書き写す**
→ 次の月、同じ取引先でまた同じ質問をする

**この往復のすべてが手作業で、しかも記録が残らない。**

### 4.2 設計

**取引の行から直接、質問を投げられる。**

- ①未仕訳の明細 / ⑩資料の受け取り の各行に **[顧客に聞く]** ボタン
- 質問を書くと、**顧客側（RIBRE）の画面に「税理士さんからの質問」として出る**
- 顧客が答えると、その答えが**その明細の行に貼り付く**
- 税理士は答えを見て登録する。**答えは摘要欄にワンクリックで入れられる**

保存先（新テーブル）:

```sql
create table if not exists tax_questions (
  id             bigint generated by default as identity primary key,
  transaction_id text,                    -- 明細に紐づく質問（nullable）
  journal_id     text,                    -- 登録済み仕訳への質問（nullable）
  evidence_id    bigint,                  -- 証憑への質問（nullable）
  asked_by       text not null,           -- 質問した税理士
  question       text not null,
  answer         text,
  answered_by    text,
  answered_at    timestamptz,
  status         text not null default 'open',  -- open / answered / closed
  created_at     timestamptz not null default now()
);
-- RLSポリシーは作らない（service role のみ。既存テーブルと同じ方針）
```

### 4.3 顧客側（RIBRE）の画面

**新規ページは作らない。** 既存の証憑インボックス（`mf-evidence.html`）の
最上部に「**税理士さんからの質問（3件）**」のパネルを出す。
未回答があるときだけ表示する。

⚠ 顧客に新しい画面を覚えさせない。**これは導入のしやすさに直結する。**

### 4.4 同じ質問を繰り返さない（学習）

同じ取引先で過去に答えがあれば、質問する前に表示する:

> 「ｱﾏｿﾞﾝ ｼﾞｬﾊﾟﾝ — 前回（6/14）の回答: 『梱包資材の購入です』（回答者: 佐渡）」

これは `tax_questions` を取引内容のテキストで引くだけで実現できる。
**Phase 4 の「過去の仕訳から科目を提案する」と同じ考え方**で、
提案の根拠として「前回はこう答えていました」を出す。

### 4.5 通知

- 顧客側: 未回答があるとき、証憑インボックスにバッジ。
  **Chatwork通知は既存インフラ（`CHATWORK_API_TOKEN`）で送れる**が、
  質問1件ごとに飛ぶとうるさいので **1日1回まとめて**送る
- 税理士側: 回答が付いたら「回答が届きました（3件）」をタブのバッジで出す

---

## 5. ★E 登録の取り消し（⑥の工程）

### 5.1 これができると何が変わるか

Phase 6 までは、登録ボタンの上に必ずこう書いていた:

> 「登録するとMFクラウド会計に仕訳が作られます。**この画面から取り消すことはできません。**」

新人レビューが一貫して挙げていた最大の不安がこれである。
しかし **`DELETE /api/v3/journals/{id}` は存在する**（仕様で確認。204を返す）。

### 5.2 設計（強い制限をかける）

**登録の直後にだけ、自分が登録した仕訳を取り消せる。**

取り消せる条件（**すべて**満たすときだけボタンを出す）:

1. **この画面から登録した仕訳**であること（`tax_advisor_actions` に記録がある）
2. **自分が登録した**もの（admin は事務所全員の分を取り消せる）
3. 登録から **30分以内**
4. **証憑をまだ添付していない**こと ← 下記の理由により必須
5. 決算済み期の設定が `block` ならその期は不可

⚠ **条件4が絶対に必要な理由**: MFの証憑（vouchers）は
**アップロードすると取り消せない**（`CLAUDE.md` 制約10）。
`DELETE /vouchers` は添付解除だけでファイル本体は残る（電帳法要件）。
仕訳だけ消すと**Box に紐付け先の無いファイルが残る**。
したがって**証憑を添付した仕訳は取り消しの対象外**とし、画面にもそう書く。

⚠ **未確認（P7-B）**: 仕訳を削除したとき、元の明細（transaction）の
仕訳化ステータスが `registered` から `none` に戻るかどうかは**仕様に記載が無い**。
戻らなければ、その明細は「仕訳済みだが仕訳が無い」宙ぶらりんになる。
**実測してから実装する。**戻らない場合はこの機能を出さない
（「MFの仕訳帳から削除してください」の案内に戻す）。

### 5.3 記録

取り消しも `tax_advisor_actions` に記録する（`action: 'delete_journal'`）。
**元の登録記録は消さない。** 「登録した」「取り消した」の2行が残る。

---

## 6. ★D 現金払い（Phase 5 ⑦の設計を作り直す）

### 6.1 Phase 5 の設計の問題

Phase 5 は「領収書をOCRして `POST /journals` で仕訳を直接作る」設計だった。
これには借方・貸方を両方この画面で決める必要があり、
**この画面が仕訳エディタになってしまう**（＝MFをなぞる方向）。

### 6.2 作り直し

**`POST /api/v3/transactions` で「明細」を作る。**

`ConnectedAccount` には **`is_manual`（明細を手動管理する連携サービスかどうか）**
というフィールドがある（仕様で確認）。MFには「現金」等の手動管理口座を作れる。

> 領収書をOCR → **手動管理の口座に明細を1件作る**（日付・金額・取引内容・支出）
> → **その明細が①未仕訳の明細タブに出てくる**
> → **あとは既存の登録フローとまったく同じ**（提案・証憑添付・操作履歴・承認）

**利点:**

- この画面に仕訳エディタを作らなくてよい（借方の科目だけ選べばよい。既存と同じ）
- MFの画面から見ても**普通の未仕訳明細**なので、税理士がMF側で処理してもよい
- 貸方は連携口座に紐づく科目でMFが決める → **「事業主借か役員借入金か」を
  この画面で迷わせない**（Phase 6 §3.5 の悩みが消える）
- Phase 5 の未確認事項 **P5-A/B/C（消費税の丸め）が不要になる**。
  税額は既存の `journalize` と同じくMFが計算する

### 6.3 前提（未確認 P7-C）

- MF側に**手動管理の連携サービス（現金など）が存在すること**。
  `GET /connected_accounts` の `is_manual: true` を探す。無ければ
  **MFの画面で作ってもらう必要がある**（この画面からは作れない。APIが無い）
- `POST /transactions` で作った明細に `transaction_id` が返り、
  それが `POST /transactions/journalize` で使えること

⚠ **どちらも実測してから実装する。** 現時点では設計の見通しであって確定ではない。

---

## 7. Phase 6 からの引き継ぎと位置づけの変更

Phase 6（3視点レビュー2巡を通過済み）の内容は**すべて維持する**。ただし:

- **一括確認リスト**は「MFに速さで勝つ機能」ではなく
  「**資料が揃った分をまとめて片付ける機能**」と位置づけ直す。
  ⑩資料の受け取りで「資料あり」に絞った状態から一括確認へ入る導線を主にする
- **事務所の管理・承認ワークフロー**はそのまま
- **用語の説明・文言整備**はそのまま。★A〜Eの用語（質問・資料待ち）も追加する
- Phase 6 §10 の追加要件（失敗カウント・20行上限・承認時の内容突合など）もそのまま

---

## 8. タブ構成（最終）

```
[ 毎日・毎週 ]  ⑩ 資料の受け取り   ① 未仕訳の明細   ⑥ 証憑の紐付け   （質問と回答）
[ 月末 ]        ⑨ 月次チェック     ⑧ 証憑なし仕訳   ⑦ 現金払い
[ 資料と管理 ]  ② 仕訳待ちの証憑   ③ 共有ファイル   ④ 操作履歴
                ⑤ 税理士の管理(RIBREのみ)   事務所の管理(adminのみ)   （承認待ち※）
```

- **⑩資料の受け取りが最初のタブ**。「今日やること」がここに集まる
- ⑨月次チェックは月末に開くもの。日常のタブと分ける
- ※承認待ちは `approval_policy='required'` のときだけ

---

## 9. 未確認事項（実装前に必ず実測する）

| # | 内容 | 実測しないとどうなるか | 代替案 |
|---|---|---|---|
| **P7-A** | `GET /transactions` の `voucher_file_ids` が実データで埋まるか | ★A の「資料が無い明細」が全件誤判定になる | 台帳 `mf_evidence` 側から逆引き（§3.2） |
| **P7-B** | 仕訳を削除したとき明細が `none` に戻るか | ★E で明細が宙ぶらりんになる | 戻らないなら**★Eを出さない** |
| **P7-C** | `is_manual` の連携サービスが存在するか／`POST /transactions` の明細が `journalize` できるか | ★D が成立しない | MFの画面で手動口座を作ってもらう |
| **P7-D** | `report.read` スコープをアプリのトークンで取得できるか（MCPでは取れている） | ★C が作れない | — |
| P6-A | MFの仕訳詳細画面への直接URLの形式 | 成功メッセージからリンクできない | 仕訳番号の案内で代替 |

⚠ P5-A/B/C（現金払いの消費税の丸め）は **★D の作り直しにより不要になった**。

---

## 10. 実装の順序

**危険度が低く価値が高いものから。**

| 順 | 内容 | 書き込み | 理由 |
|---|---|---|---|
| 1 | スコープ追加＋再連携（5つまとめて） | — | 再連携を1回で済ませる |
| 2 | **★C 月次チェック** | **無し（読み取りのみ）** | 最も安全で最も価値が高い。実データで効果を検証済み |
| 3 | P7-A/B/C の実測 | 無し | 以降の設計の前提 |
| 4 | **★A 資料の受け取りボード** | 無し（★Bを除く） | ①の工程。税理士の待ち時間を減らす |
| 5 | **★B 質問と回答** | 自社DBのみ | ④の工程。**この設計の中核** |
| 6 | 共有ファイルの証憑／参考資料の区分（Phase 5 §1） | 自社DBのみ | 給与明細のプライバシー問題。優先度は高い |
| 7 | Phase 6 の文言・用語整備 | — | 実装量が小さい |
| 8 | Phase 6 の一括確認リスト | MF（既存API） | ★Aの絞り込みから入る導線として |
| 9 | Phase 6 の admin/staff・承認 | 自社DBのみ | 所長レビューの導入条件 |
| 10 | **★E 取り消し** | MF（DELETE） | P7-B の結果しだい |
| 11 | **★D 現金払い** | MF（POST transactions） | P7-C の結果しだい |

⚠ **2〜5 までで、税理士にとっての価値の大半が出る**（①④⑤の工程が埋まる）。
そこまでは**MFへの書き込みが一切発生しない**。

---

## 11. 3視点レビュー1巡目（2026-08-04）の反映

結果は **3人とも B（条件が満たされれば使いたい／導入する）**。
方向転換そのものは3人とも「正しい」と評価した。
A に届かない理由は**共通して「未確認事項 P7-A〜D が実測されていないこと」**で、
これは設計の修正では解消できない（§13 参照）。

以下は**設計の修正で解消できる指摘**をすべて反映したもの。

### 11.1 ⑨月次チェック — 「月の途中」と「計上漏れ」を区別する（最重要）

新人レビューの指摘:

> 7月が0円なのは「まだ月の途中だから」なのか「本当の計上漏れ」なのか、
> この画面だけでは区別がつかない

**これは設計の穴だった。** §2 の判定は当月が0であることしか見ておらず、
「登録作業がまだ終わっていないだけ」と区別できない。実際 §2.1 に載せた実データも、
7月の7科目が0なのは**登録がまだ途中だから**である。

修正:

1. **月次チェックの先頭に、その月の未仕訳明細の残件数を必ず出す**

   > 「**7月の未仕訳明細が42件残っています。**
   >  登録が終わっていない月では、下の『まだ無い科目』は当然の結果です。
   >  まず①未仕訳の明細を片付けてから、このチェックを見てください。」

   これで**データだけで区別がつく**（残件0なら計上漏れの疑いが濃い）。
   残件が0でなければ、計上漏れの判定は**警告色を出さず参考表示にとどめる**。

2. **進行中の月は計上漏れ判定を行わない。**
   対象月の末日を過ぎていない場合（例: 8/4に8月を見る）は
   「2026年8月はまだ月の途中です。計上漏れの判定は月が終わってから行います」と表示し、
   ①のリストを出さない（②金額異常・③符号異常は参考として出してよい）。

3. 判定結果の各行に、その科目の**当月の未仕訳明細の件数**を併記する。
   「地代家賃 — 過去4ヶ月は毎月198,000円。7月は0円。（未仕訳明細に候補が1件あります）」

### 11.2 ⑨月次チェック — 「出ていない＝問題なし」を防ぐ（所長）

所長レビューの指摘:

> 職員は「ここに出ていないから大丈夫」という誤読をしやすい。
> 機械が拾わない外側の誤りに対する緊張感がむしろ下がる

修正:

1. **検知できない範囲を画面に明示する**（判定条件と並べて、同じ大きさで書く）

   > **このチェックで見つけられないもの**
   > ・初めて使った勘定科目への誤った計上（比べる過去がないため）
   > ・過去4ヶ月分のデータが揃っていない科目（期首直後など。**判定していません**）
   > ・しきい値未満のゆるやかな増減（中央値の2倍程度など）
   > ・金額は正しいが**科目や税区分が間違っている**もの
   > **ここに何も出ないことは「問題なし」を意味しません。**

2. **人間が確認した記録を残す**（所長の要望そのもの）

   月次チェックの最後に確認ボタンを置く:

   > ☐ この月について、上のフラグの有無にかかわらず**全科目をひと通り確認しました**
   > 　［確認を記録する］

   押すと `tax_advisor_actions` に `action: 'monthly_check_confirmed'` として
   **誰が・いつ・どの月を確認したか**を記録する。
   **機械の出力を見た記録ではなく、人間が確認した記録を残す**のが目的。
   CSVエクスポートの対象にも含める。

3. ②金額異常・③符号異常にも「**該当する取引をさがす**」ボタンを付ける
   （①にしか無く、新人が次に何を見ればよいか分からないという指摘）。
   遷移先は①未仕訳の明細（科目で絞り込み）と、MFの仕訳帳（該当月・該当科目）の2つ。

### 11.3 絞り込みが実際に効くこと（中堅・実測による指摘）

中堅レビューは実機で**ボタンが遷移するだけで絞り込まれない**ことを確認した。
モックの都合だが、**実装の必須要件として明記する**:

1. ⑨の「未仕訳明細をさがす」→ **①タブを、その勘定科目の候補だけに絞り込んだ状態**で開く
2. ⑩の「**資料はあるが未仕訳 5件**」→ **①タブを、証憑がある明細だけに絞り込んだ状態**で開く
   （§7 で「⑩から一括確認へ入る導線を主にする」と書きながら、
   モックにボタンが無かった。中堅の指摘どおりの実装漏れ）
3. 「顧客に聞く」→ **質問フォームにその取引が選択済みで**開く
   （3人中2人が「連動しないと『行から直接聞ける』が嘘になる」と指摘）

⚠ **絞り込みが効かない実装は、この3機能の価値の大半を失わせる。**
実装時のテスト項目に入れる。

### 11.4 ⑦現金払い — 既存運用との重複（中堅。見落としていた）

中堅レビューの指摘:

> `CLAUDE.md` に「**現金レシート → MFクラウド経費のスマホアプリ（このシステムの守備範囲外）**」
> という取り決めがすでにある。⑦は同じ問題を別経路でもう一度作ろうとしている

**そのとおりで、これは見落としだった。** Phase 5 でも Phase 7 でも、
この既存の取り決めとの関係を一度も検討していない。

修正: **⑦の位置づけを「作る機能」から「作るか決める前に確認すること」へ格下げする。**

実装順の最後（11番目）は変えず、その前に次を確認する:

1. RIBRE は現在、現金レシートをMFクラウド経費アプリで処理できているか
2. できているなら **⑦は作らない**（同じことを2経路で作ると現場が迷う）
3. できていない場合、その理由は何か（アプリを使っていない・連携していない等）。
   理由によっては**MF経費アプリの設定を直すほうが正しい**

⚠ **この確認より先に P7-C（`is_manual` 口座の実測）を行わない。**
作らないと決まれば実測自体が不要になる。

### 11.5 質問と回答の証跡（所長）

> この設計の中核と位置づけながら、CSVエクスポート（証跡の独立コピー）が
> 操作履歴にしか用意されていない

修正: **`tax_questions` にも操作履歴と同等の月次CSVエクスポートを付ける。**
質問・回答・質問者・回答者・日時・対象の明細IDを含める。

**正直に書くこと（自動送信では解決しない）:**
「事務所へ自動でメール送信すれば安全」ではない。**送信元がRIBRE側である以上、
改ざん検知の強さは変わらない。**担保になるのは
**事務所が自分でダウンロードして、RIBREが触れない場所に保管すること**だけである。
ガイドにこのとおり書く。

### 11.6 ベンダー依存への回答（所長。Phase 5 から未解決の論点）

所長レビューは「顧問先が作ったシステムに事務所が全面的に依存する」構造を
一貫して問題視している。これは解消できないが、**Phase 7 の立ち位置では影響が小さい**
ことを明記していなかった。

> **この画面が止まっても、事務所の業務は止まらない。**
> 仕訳はMFクラウド会計に入り、証憑はMFクラウドBoxに入る。この画面には残らない。
> 止まった場合はMFで従来どおり作業できる。
> **失われるのは「質問と回答の履歴」と「月次チェックの確認記録」だけ**であり、
> だからこそ §11.5 の月次エクスポートが必要になる。

これをガイドの冒頭と、画面の「この画面の立ち位置」パネルに書く。

### 11.7 そのほかの修正

| # | 指摘 | 修正 |
|---|---|---|
| 1 | 取り消しボタンに確認ダイアログが無い（新人） | 登録実行と同じく確認を1回はさむ |
| 2 | 「証憑不要にする」の取り消し可否が書かれていない（新人） | **取り消せる**ようにし、操作履歴に残すことを明記 |
| 3 | 「対象外」と「証憑不要」が紛らわしい（新人） | 用語集に違いを追加。対象外＝MFの機能（この画面では操作できない）／証憑不要＝この画面だけの印 |
| 4 | タブ群が常時218px占有（中堅・実測） | 「月末」「資料と管理」の2段を**初期は折りたたむ**（開閉は端末に記憶） |
| 5 | 承認は新人期間に必須にすべき（所長・新人） | ガイドに「新人・担当者は導入後3〜6ヶ月は `approval_policy='required'` を推奨」と明記 |
| 6 | 実測結果を事務所へ再提示してから合意（所長） | §13 の手順に組み込む |

---

## 12. 「見つけられないもの」を先に書く（設計の原則として追加）

今回のレビューで所長・新人の双方が同じ懸念を挙げた:
**機械が何かを出す画面は、「出なかったこと」を安全の証明だと誤解させる。**

そこで Phase 7 以降の原則として、**自動判定を行うすべての画面に
「この判定で見つけられないもの」を、判定条件と同じ大きさで併記する**。

対象:
- ⑨月次チェック（§11.2）
- ①の勘定科目の提案（過去5件のうち何件、という根拠は既に出しているが
  「**過去に無い取引には提案が出ない＝提案が無いことは異常ではない**」を明記）
- ⑩資料の受け取り（「**現金払いはそもそも明細に現れないため、ここには出ません**」）
- ⑥証憑の紐付け（「日付・金額が一致しない証憑は候補に出ません」）

---

## 13. A に到達するために必要なもの（設計では解消できない）

3人の A 条件を突き合わせると、**共通して残るのは実測だけ**である。

| # | 実測すること | 必要な作業 | 誰が |
|---|---|---|---|
| **P7-D** | `report.read` をアプリのトークンで取得できるか | **MF連携のやり直し**（スコープ5つを追加して1回） | 利用者 |
| **P7-A** | `GET /transactions` の `voucher_file_ids` が実データで埋まるか | 上記の再連携後、ログインして確認 | 利用者＋開発 |
| **P7-C** | `is_manual` の連携サービスが存在するか | 同上。**ただし §11.4 の運用確認が先** | 利用者 |
| **P7-B** | 仕訳を削除したとき明細が未仕訳に戻るか | **MFに実際に仕訳を1件作って消す**。本番データを触るため**利用者の明示的な許可が必要** | 利用者 |

⚠ P7-D が最優先。**価値が最も高い⑨月次チェックが、これ1つに懸かっている**（中堅の指摘）。
再連携1回で P7-A・P7-C・P7-D がまとめて確認できる。

⚠ P7-B だけは性質が違う。**本番の帳簿に仕訳を作って消す**必要があり、
利用者の許可なしには行わない。許可が得られない場合は
**★E（登録の取り消し）を設計から落とす**（新人の最大の不安は解消できないまま残る）。

**実測が終わったら、結果を反映した設計とモックを3人へ再提示する**（所長の条件5）。

---

## 14. 3視点レビュー2巡目（2026-08-04）の反映

結果: **中堅 A（新規指摘なし）／新人 B（残り1点）／所長 B（残り3点）**。
実測（P7-A〜D）を判定から除外した条件付きでの評価。

### 14.1 見つかった不備（新人と所長が独立に発見）

**月次チェックの「確認を記録する」ボタンが、チェックボックスと連動していなかった。**

`§11.2-2` で追加した「人間が確認した記録」は、所長が
「機械の出力を見た記録ではなく、人間が確認した記録を残す」ために要望した機能である。
ところがモックの実装では、**チェックを入れなくてもボタンが押せ、
「全科目をひと通り確認しました」という記録が残った**。

> 所長:「これでは以前と同じ『思考停止』を、確認したという記録つきで追認するだけになる」
> 新人:「忙しい新人が中身を見ずに押しても監査ログ上は『確認済み』になる。
> 　　　 あとで問題が起きたとき『本当に確認したのか』を証明する材料にすらならない」

**修正**（実装時も同じ扱いにすること）:

1. チェックが入るまでボタンを `disabled` にする
2. **サーバー側でも**チェックの有無を受け取り、未チェックの記録は受け付けない
   （画面だけの制御にしない。この記録は監査証跡なので、経路を1つに絞る）
3. ボタンの下に「チェックを入れずに記録することはできません。実際に確認していないときは
   押さないでください」を常時表示する

⚠ この機能は**押させることが目的ではなく、押したことが本当である**ことが目的である。
実装時に「便利だから」と自動化・省略化しないこと。

### 14.2 §12 の適用漏れ（所長）

§12 は「⑨月次チェック・①提案・⑩資料の受け取り・**⑥証憑の紐付け**」の4箇所を名指ししたが、
モックでは⑥だけ実装されていなかった。

> 所長:「証憑の紐付けは、日付・金額がずれた証憑を見逃すと
> 『証憑が無いから気づく』という他のセーフティネットも働かない箇所であり、
> むしろ4つの中でも書き漏らしてはいけない場所」

**修正**: ⑥に追加した。「日付や金額が一致しない証憑は候補に出ません」
「外貨建ての証憑は金額での照合の対象外です」
「**左の一覧が空になっても、すべての証憑がつながったとは限りません**」。

### 14.3 「止まったとき」の範囲が不正確だった（所長）

§11.6 で「失われるのは質問と回答の履歴と月次チェックの確認記録だけ」と書いたが、
**④操作履歴（誰が・いつ・どの根拠で登録／承認／取り消しをしたか）も同じDBにしかない**。

> 所長:「仕訳そのものはMFに残るとしても、『誰が承認して登録したか』
> 『取り消しの経緯』という**事務所としての説明責任の記録**は、
> 質問と回答・月次確認記録と全く同じ運命にある。
> これを『失われるのは2つだけ』と限定して書くのは、リスク開示として不正確」

**修正**: 「この画面にしか残らないもの」を**3つ**（質問と回答／月次チェックの確認記録／
**④操作履歴**）と正しく書き直し、**3つとも月次でCSVに書き出す**ようガイドに書く。

### 14.4 そのほか

| # | 指摘 | 修正 |
|---|---|---|
| 1 | ⑦タブのチップが「作り直し（Phase 5から設計変更）」のままで、格下げ後の位置づけと矛盾（所長） | 「作るかどうか未定（既存運用と重複の可能性）」へ |
| 2 | 承認の推奨文言に「3〜6ヶ月」とadmin限定が無い（新人。§11.7-5との差分） | 両方を画面の文言に反映 |
| 3 | 月セレクタで6月・5月を選んでも表が変わらない（新人） | モックの都合である旨を画面に注記（実装ではAPIから月ごとに取り直す） |

### 14.5 中堅の判定（A）と削減時間の更新

中堅は**新規の指摘なし・A**。前回「動かない」と実機で指摘した3箇所（⑨からの絞り込み・
⑩からの絞り込み・「顧客に聞く」の連動）が**実際に動くこと**を自分でクリックして確認した。

削減時間の見積もりは **月10分前後 → 月15分前後** に更新された
（絞り込みが動く前提が確認できたため、前回の割引が外れた）。
**担当20社なら月5時間相当**。

> 中堅:「⑨残件数の警告は直接の時短ではないが、
> **これがないと機能自体が信用されず使われなくなる**ので、他の項目の効果を成立させる前提条件」

---

## 15. 3視点レビュー3巡目（2026-08-04）— 全員 A

| 立場 | 1巡目 | 2巡目 | **3巡目** |
|---|---|---|---|
| 新人 | B | B（残り1点） | **A（ぜひ使いたい・新規指摘なし）** |
| 中堅 | B（月10分） | **A（月15分・新規指摘なし）** | — |
| 所長 | B | B（残り3点） | **A（ぜひ導入したい・新規指摘なし）** |

**設計とモックとしては3人とも合格。** 残るのは実測（P7-A〜D）と運用の取り決めだけ。

### 15.1 3巡目で確認されたこと

新人・所長とも、**ボタン経由とJS直叩きの両方**で §14.1 の修正を検証した:

- ボタンは初期 `disabled`。無効なボタンへの `.click()` では何も起きない
- `monthlyConfirmRecord()` を直接呼んでも、未チェックなら警告して**記録しない**
- チェックを入れると有効化され、操作履歴が**過不足なく1件だけ**増える

新人の評価:

> ボタンのHTML自体に `disabled` 属性が静的に書かれているため、
> 万一JSが読み込まれなくても「押せてしまう」側には壊れず
> **「押せないまま」側に壊れる（fail-safe）**設計になっている点も良い

⚠ **実装時もこの性質を保つこと。** 初期状態を「押せる」にして
JSで無効化する作り方にしてはいけない。

### 15.2 所長が示した導入の条件（運用の取り決め）

1. **P7-D（`report.read` の再連携）を最優先**で実施してから着手する
2. **P7-B は所長へ一報してから**。許可なしに本番帳簿へ書き込まない。
   戻らないと判明した場合は **★E を出さない**ことに同意済み
3. **admin は所長本人か最小人数に限定**
4. **新人・担当者は導入後3〜6ヶ月、`approval_policy='required'` をON**
5. **質問と回答／月次チェックの確認記録／④操作履歴の3つを、
   月次で事務所側が自分でCSVをダウンロードし、RIBREが触れない場所に保管**
6. **⑦現金払いは、MFクラウド経費アプリで足りているかの確認が先**。足りているなら作らない
7. **実測が終わったら最終版を事務所へ再提示**する

### 15.3 中堅が示した効果（実測が良好だった前提）

**1社あたり月15分前後**（前回の月10分から更新。絞り込みが実際に動くことを
自分でクリックして確認したため、前回の割引が外れた）。**担当20社なら月5時間相当**。

> 中堅:「⑨残件数の警告は直接の時短ではないが、
> **これがないと機能自体が信用されず使われなくなる**ので、他の項目の効果を成立させる前提条件」

### 15.4 次にすること

`docs/TAX_WORKSPACE_PHASE7_MEASURE.md`（実測の手順書）を参照。
**利用者にお願いするのは3つだけ**（うち必須は1つ）。

---

## 16. 実測の結果（2026-08-04・本番のトークンで実施）

`api/mf/measure.js` + `mf-measure.html` で、アプリのトークンを使って実測した。
**すべて読み取りのみ。MFにもDBにも書き込んでいない。**

| # | 結果 | 内容 |
|---|---|---|
| **P7-D** | **○ 使える** | `GET /reports/transition_pl` が200。推移表12行。**⑨月次チェックを作れる** |
| **offices.read** | **○ 使える** | `term_settings` が3期分返る。2026年度=2026-03-01〜2027-02-28・`SIMPLE`（簡易課税） |
| **P7-C** | **△ 受け皿が無い** | 連携サービス9件は**すべて `is_manual: false`**。⑦を作るならMFの画面で手動口座を1つ作る必要がある |
| **P7-A** | **× 使えない** | 直近90日の明細**295件すべてで `voucher_file_ids` が空**。`new_voucher_attached` は0件 |

### 16.1 P7-A が使えない理由（原理的なもの。実装では回避できない）

これは「たまたま今は空」ではない。**構造的に埋まらない。**

`CLAUDE.md` 制約10 と `api/mf/_lib/mf-client.js` の既存コメントに既に書かれていた:

> 明細に証憑を添付するAPIは存在しない（`PostVouchersRequest` は `journal_id` のみで
> `transaction_id` を受け付けず、`Transaction.voucher_file_ids` は取得専用）

つまり **このシステムの証憑添付はすべて「仕訳（journal）」に対して行われる**ので、
「明細（transaction）」側の `voucher_file_ids` は**永久に空のまま**である。
（MFの画面から明細に直接添付した場合のみ埋まる。実測で0件＝その運用はしていない）

⚠ **設計時にこの既存の記述を見落としていた。** §3.2(a) を書いた時点で、
同じリポジトリの `CLAUDE.md` に答えが書いてあった。

### 16.2 ⑩の判定方法を差し替える（代替案の方が良い）

§3.2 の代替案（台帳 `mf_evidence` から逆引き）へ切り替える。
**結果的にこちらの方が目的に合っている。**

**新しい判定方法**: 台帳 `mf_evidence` の証憑（OCR済みの日付・金額・取引先を持つ）と、
**未仕訳の明細**を日付・金額で突き合わせる。
既存の `mf-match-core.js`（証憑→仕訳のマッチング）を明細向けに転用するだけ。

**なぜこちらの方が良いか**:

- `voucher_file_ids` は**仕訳を作った後にしか埋まらない**。
  しかし税理士が知りたいのは「**登録する前に、資料が揃っているか**」である。
  台帳との突き合わせなら**仕訳を作る前に判定できる**
- 台帳の `status='awaiting_match'`（証憑は届いたが仕訳がまだ）は、
  そのまま **「資料はあるが未仕訳」** の件数になる（モックの統計そのもの）

**⚠ この方法で見つけられないもの（§12 の原則により画面に明記する）**:

- **このシステムを通っていない証憑は分からない。**
  紙で税理士へ直接渡した領収書は、台帳に無いので「資料が無い」と表示される
- 現金払いはそもそも明細に現れないため、ここには出ない（既に明記済み）

⚠ RIBREの運用は「**毎月まとめて紙で渡す**」（2026-08-04 利用者に確認）。
したがって**⑩の精度は、証憑がこのシステムを通る割合に比例する**。
これを画面に正直に書く。「12件」という数字を、実態より多い催促リストとして
見せてはいけない。

### 16.3 実測で分かったその他の事実

- **直近90日の未仕訳明細は95件**（`journalizing_statuses=none`）。
  仕訳済みは200件以上（取得上限に当たったため正確な数は未計測）
- 8/1 の明細がすでに入っている＝**銀行・カードの連携は正常に動いている**
- 明細の取得は **`journalizing_statuses` を必ず指定する**こと。
  指定せず全件を取ろうとするとMFが500（`internal_error`）を返す（実測）。
  MF公式MCPサーバー経由でも同じ症状が出ていた

### 16.4 P7-B（登録の取り消し）は未実測

本番の帳簿に仕訳を作って消す必要があるため、**利用者の許可待ち**。
許可が得られない場合は ★E を設計から落とす（§13 のとおり）。

### 16.5 実測を踏まえた実装順序の更新

| 順 | 内容 | 状態 |
|---|---|---|
| 1 | スコープ追加＋再連携 | **完了**（read 3つ。書き込み系は足していない） |
| 2 | **★C ⑨月次チェック** | **着手できる**（P7-D ○） |
| 3 | ★A ⑩資料の受け取り（**台帳との突き合わせ方式**へ変更） | 着手できる（P7-A の代替案） |
| 4 | ★B 質問と回答 | 着手できる（自社DBのみ） |
| 5 | 共有ファイルの証憑／参考資料の区分 | 着手できる |
| 6 | Phase 6 の文言・一括確認・承認 | 着手できる |
| 7 | ★E 取り消し | **P7-B の許可待ち** |
| 8 | ★D ⑦現金払い | **手動口座の作成＋現金の記帳方法の確認待ち** |

---

## 17. 「役員借入金」は存在しない科目だった（2026-08-04・利用者の指摘）

利用者からの指摘:

> 現金払いのやつってもしかして現在は、全部役員借入金になってない？

**実測した結果、答えは「なっていない」。そして問題はこちらの側にあった。**

`GET /accounts`（勘定科目マスタ・199科目）を全件確認したところ:

| 探した語 | 結果 |
|---|---|
| **役員借入** | **無し** |
| **事業主** | **無し** |
| 現金 | `現金` / `小口現金` |
| 未払金 | `未払金` / `長期未払金` |
| 短期借入 | `短期借入金` |

**`役員借入金` はこの事業者の勘定科目マスタに存在しない。** 試算表の負債の部にも出てこない。
現金払いは `現金` 勘定で処理されている（推移表BSで現金残高が毎月動いていることと整合）。

### 17.1 何を間違えたか

- Phase 6 §3.5 で「例に使っていた `事業主借` は個人事業主の科目なので、
  法人であるRIBREには不適切」と気づき、**`役員借入金` に置き換えた**
- しかしその置き換え先が**実在するかを確認しなかった**
- **同じ §3.5 に「選択肢はMFの勘定科目マスタから実在するものだけを出す」と自分で書いていた。**
  ルールは書いたのに、例示のほうに適用していなかった
- 結果、モック（Phase 6・Phase 7）と用語集に、**存在しない科目**が載っていた

### 17.2 直し方

1. **例示をやめる。** 貸方の候補は**マスタから取ったものだけ**を出す（ルールどおりにする）
2. 用語集から `役員借入金` を削除する
3. ⑦を作る場合の貸方は、**MFが連携口座から自動で決める**（§6.2）ので、
   そもそもこの画面で貸方を選ばせない。例示自体が不要だった
4. **設計原則に追加**: 設計書やモックに**具体的な勘定科目名を書くときは、
   必ず勘定科目マスタで実在を確かめてから書く。**
   「一般的にはこう」で書かない（事業者ごとにマスタが違うため）

### 17.3 あわせて直した不具合（同じ指摘の中で）

> 共有ファイルをダウンロードすると名前が違うファイル名になる

③共有ファイルの署名URLに `download=` を付けていなかったため、
保存時の内部キー（日時つきの名前）でダウンロードされていた。
**`api/mf/evidence-action.js` は同じ理由で先に対応済みだったのに、
`api/mf/tax-workspace.js` だけ抜けていた**（同じリポジトリ内での不統一）。
元のファイル名を渡すよう修正。
